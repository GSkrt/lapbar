#!/usr/bin/env python3
"""Launcher used by the Quickshell widget; runs without installation and without a venv."""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.realpath(__file__)), ".."))

from lapbar.cli import main  # noqa: E402

main()
